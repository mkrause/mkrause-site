
var path = require('path');

module.exports = {
    env: 'production',
    logDir: '/root/mkrause_site/logs',
    postsDir: path.join(__dirname, '../posts'),
    secret: 'super-secret', // Replace this with some strong random string
    accounts: {}
};
